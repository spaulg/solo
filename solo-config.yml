logging:
  # Enable debugging during development
  level: "debug"

entrypoint:
  # Override entrypoint to use wrapper script for running via go run
  host_entrypoint_path: "./cmd/solo-entrypoint/solo-entrypoint-wrapper.sh"
